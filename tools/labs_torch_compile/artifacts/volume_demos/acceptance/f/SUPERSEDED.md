# Superseded evidence

This directory preserves the original Volume F acceptance run. Its
`ddp_compile` failure and aggregate `summary.json` status were superseded by
the complete corrected rerun in `../f-fixed/`.

Use `../f-fixed/summary.json` as the canonical Volume F result: **4 PASS,
4 BLOCKED, 0 FAIL**.
