# Superseded evidence

This directory preserves the original Volume E acceptance run. Its
`minifier_repro` result and aggregate `summary.json` status were superseded by
`../e-minifier-fixed/`, where the corrected case passed.

For the canonical Volume E roll-up, use the eight unaffected results here plus
the replacement `minifier_repro` result in `../e-minifier-fixed/`: **7 PASS,
2 BLOCKED, 0 FAIL**.
