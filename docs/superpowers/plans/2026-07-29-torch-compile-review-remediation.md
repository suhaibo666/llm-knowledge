# Torch.compile 图系列审查修订计划

> 状态：已完成。此计划只记录本轮修订路径，不纳入最终提交。

## 目标

以 PyTorch 固定基线 `e8f97c1a6ef8cbcdd0a946606bc1e924e4f07e52` 为唯一源码事实源，修复审查指出的深度不均、Markdown 渲染、源码 checkout 漂移、超宽定位与证据歧义，同时保持已有内容和用户工作区不丢失。

## 修订范围

1. 保留含未提交内容的 `E:/97-codes/torch_parallel/pytorch`，复用独立且 clean 的精确基线 checkout `E:/97-codes/torch_parallel/p`，并在课程入口和审计材料中明确本地跳转路径。
2. 以 fence-aware 方式修复课程 Markdown 中缺空格的列表项，修正 B07 公式，并增加回归检查。
3. 为 B04、B07、D01、D02、D06、F01 增加“源码跟读”：
   - 调用入口与下游出口；
   - 状态、缓存、对象所有权和 ABI；
   - 关键分支为何存在；
   - 失败路径与可观测现象；
   - 不超过 30 行的精确源码锚点。
4. 枚举 C17–C21 中超过 100 行的定位，按具体类、方法、分支收窄；不以任意切片冒充语义定位。
5. 为 C 卷关键架构/数据流补 Mermaid，保留原有 ASCII 图和解释，避免信息损失。
6. 为被修复运行取代的旧 FAIL 证据增加 superseded 标记，保留原始证据。
7. 重建 claim ledger、manifest 与交付报告，执行全量源码定位、wikilink、Markdown 结构、demo contract 和审计测试。

## 验收标准

- 课程 Markdown 中代码围栏外 `^-[^ \t-]` 为 0。
- B07 公式可正常渲染。
- 六篇优先页面均具有可顺序执行的机制级源码跟读章节。
- 课程内不存在超过 100 行的源码定位；新增 `[S]` 锚点不超过 30 行。
- 固定基线 checkout 可独立跳转，且不改变用户原有脏 checkout。
- 旧 FAIL 与其替代 PASS 的关系可从证据目录和交付报告直接识别。
- 全量审计与测试通过；任何环境型 BLOCKED 继续明确标注，不改写成 PASS。
